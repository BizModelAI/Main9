// Generate AI content for the preview
  useEffect(() => {
    if (personalizedPaths.length > 0) {
      // Start generating AI content immediately
      generateAIContentForPreview();
    }
  }, [personalizedPaths]);
